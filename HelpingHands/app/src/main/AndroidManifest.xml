<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.helpinghands">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppThemeInitial">
        <activity android:name=".activity.StartActivity" />
        <activity
            android:name=".activity.LoginActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".activity.OrgSignUpActivity"
            android:theme="@style/AppTheme" />
        <activity android:name=".activity.EmailActivity" />
        <activity android:name=".activity.VolSignUpActivity" />
        <activity android:name=".activity.ChooseActivity" />
        <activity android:name=".viewpager.WelcomeActivity">
            <intent-filter>
                <category android:name="android.intent.category.LAUNCHER" />

                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.ProblemsSelectionActivity" />
        <activity android:name=".activity.EventDetailsActivity"
            android:theme="@style/AppTheme"
            />
        <activity android:name=".activity.ForgotPasswordActivity"
            android:theme="@style/AppTheme"/>
        <activity android:name=".activity.ChangePassword"
            android:theme="@style/AppTheme"/>
        <activity android:name=".activity.VolunteerHomeActivity"
            android:theme="@style/AppTheme">
        </activity>
        <activity android:name=".activity.OrgHomeActivity"
            android:theme="@style/AppTheme"
            ></activity>
    </application>

</manifest>